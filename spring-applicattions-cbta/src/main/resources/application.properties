spring.application.name=spring-applicattions-cbta
spring.web.resources.cache.period=0
spring.web.resources.chain.cache=false

# Conexión a PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/cbta_calificaciones
spring.datasource.username=postgres
spring.datasource.password="cabrera2411"

# Driver de conexión
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA (manejo de entidades y tablas)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT
jwt.secret=MiClaveSecretaMuyLarga12345
jwt.expiration-ms=86400000  
# 1 día
